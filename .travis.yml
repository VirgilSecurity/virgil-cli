language: cpp
branches:
  only:
    - master
    - v2.0.0
    - openwrt
    - /^release*$/

matrix:
  include:
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-4.9
            - libcurl4-openssl-dev
      env:
        - CUSTOM_CC=gcc-4.9
        - CUSTOM_CXX=g++-4.9
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
            - libcurl4-openssl-dev
      env:
        - CUSTOM_CC=gcc-5
        - CUSTOM_CXX=g++-5
    - compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.6
          packages:
            - clang-3.6
            - libcurl4-openssl-dev
      env:
        - CUSTOM_CC=clang-3.6
        - CUSTOM_CXX=clang++-3.6
    - compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.7
          packages:
            - clang-3.7
            - libcurl4-openssl-dev
      env:
        - CUSTOM_CC=clang-3.7
        - CUSTOM_CXX=clang++-3.7
env:
  global:
    - BUILD_DIR_NAME=build
before_install:
  - bash ./ci/install-cmake.sh
install:
- export CC="$CUSTOM_CC" CXX="$CUSTOM_CXX"
before_script:
  - bash ./ci/configure.sh
script:
  - bash ./ci/run.sh
sudo: false
